#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int A, M, B;
    scanf("%d", &A);
    scanf("%d", &M);
    B = (M*2) - A;
    printf("%d", B);
	return 0;
}
